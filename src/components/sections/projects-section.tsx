import SectionLayout from "@/components/layouts/section-layout";

export default function ProjectsSection() {
  return (
    <SectionLayout
      id="projects"
      title="Projects"
      description="저는 이런 프로젝트를 진행했습니다."
      className="bg-green-100"
    >
      ProjectsSection
    </SectionLayout>
  );
}
